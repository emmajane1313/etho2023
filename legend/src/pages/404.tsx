import { FunctionComponent } from "react";

const Custom404: FunctionComponent = (): JSX.Element => {
  return (
    <div className="relative h-screen w-full bg-black font-dog text-white flex items-center justify-center">
      Lost?
    </div>
  );
};

export default Custom404;
