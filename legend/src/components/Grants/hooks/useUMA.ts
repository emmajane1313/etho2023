const useUMA = () => {
  const disputeGrant = async () => {
    try {
    } catch (err: any) {
      console.error(err.message);
    }
  };

  return {
    disputeGrant,
  };
};

export default useUMA;
