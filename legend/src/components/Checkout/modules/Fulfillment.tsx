import { FunctionComponent } from "react";
import { CartItemsProps, FulfillmentProps } from "../types/checkout.types";

const Fulfillment: FunctionComponent<FulfillmentProps> = ({
  fulfillment,
  setFulfillment,
}): JSX.Element => {
  return <div></div>;
};

export default Fulfillment;
